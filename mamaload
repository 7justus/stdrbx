--in linter will be intregrated into a scripthub and it's just ImageLabel bro 
--stop skidding this atleast 😭
local function mama()
    local ScreenGui = Instance.new("ScreenGui")
    local ImageLabel = Instance.new("ImageLabel")


    ScreenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
    ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

    ImageLabel.Parent = ScreenGui
    ImageLabel.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
    ImageLabel.BackgroundTransparency = 1.000
    ImageLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
    ImageLabel.BorderSizePixel = 0
    ImageLabel.Position = UDim2.new(0.749763489, 0, 0.412350595, 0)
    ImageLabel.Size = UDim2.new(0, 345, 0, 304)
    ImageLabel.Image = "rbxassetid://92829927042668"
end


mama()
